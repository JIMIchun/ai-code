Single-database configuration for Flask.

# 初始化迁移环境,生成目录
flask db init


# 生成迁移脚本'/migrations/versions/**.py'
flask db migrate
# 数据库迁移
flask db upgrade
# 迁移降级
flask db downgrade
-- 在脚本文件中出现create_foreign_key()，应将第一个参数由None改为列名